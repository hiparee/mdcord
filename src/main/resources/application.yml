spring:
  profiles:
    active: prod
  servlet:
    multipart:
      max-file-size: 30MB
      max-request-size: 30MB

server:
  port: 9000
  error:
    include-stacktrace: never

logging:
  level:
    org.hibernate.SQL: debug
